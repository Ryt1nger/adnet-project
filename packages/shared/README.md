# Shared

Future shared TypeScript domain types, constants, and helper contracts.

Keep this package small and dependency-light. Domain rules should live on the server unless they are pure display contracts.
